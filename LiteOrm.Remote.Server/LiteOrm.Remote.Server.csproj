<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net10.0;net8.0;</TargetFrameworks>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Description>LiteOrm 远程调用服务端</Description>
    <AssemblyVersion>8.1.5.0</AssemblyVersion>
    <FileVersion>8.1.5.0</FileVersion>
    <VersionPrefix>8.1.5</VersionPrefix>
    <PackageReleaseNotes>
8.1.5:
- 注册方式重构为「配置回调 + 自行注册选项」：除 AddRemoteServer(configure) 外，可先调用 services.AddSingleton(sp =&gt; new RemoteServerOptions { ... }) 注册选项，AddRemoteServer 内部以 TryAddSingleton 注册，已注册的实例优先（可在工厂中从 IConfiguration 读取配置）。
- 破坏性变更：移除 AddRemoteServer(string configSection, ...) 配置节重载，配置改为在 configure 回调或自行注册的选项中从 IConfiguration 读取。
- RemoteServerOptions 与 ITypeNameResolver 改为延迟解析，支持在运行时提供解析器配置。
- 同步 LiteOrm.Common 8.1.5。

8.1.4:
- 同步 LiteOrm.Common 8.1.4（Regex Lambda 支持、复杂类型映射变更、SQL 函数方言补全、字符串常量内联）。
- 增加泛型方法支持。

8.1.3:
- LiteOrm.Common 更新至 8.1.3。

8.1.2:

8.1.1:
- 改进：
  - LiteOrm.Common 更新至 8.1.1。
    </PackageReleaseNotes>
    <PackageProjectUrl>https://github.com/danjiewu/LiteOrm</PackageProjectUrl>
    <Copyright>Copyright @ 2026 danjiewu</Copyright>
    <RepositoryUrl>https://github.com/danjiewu/LiteOrm.git</RepositoryUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.Remote.Server.nuget.md</PackageReadmeFile>
  </PropertyGroup>
  <PropertyGroup>
    <LangVersion>14.0</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\README.Remote.Server.nuget.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\LiteOrm.Common\LiteOrm.Common.csproj" />
  </ItemGroup>

</Project>
