<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net10.0;net8.0;netstandard2.1;netstandard2.0;</TargetFrameworks>
    <RootNamespace>LiteOrm.Common</RootNamespace>
    <AssemblyVersion>8.1.3.0</AssemblyVersion>
    <FileVersion>8.1.3.0</FileVersion>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <GenerateDocumentationFile>True</GenerateDocumentationFile>
    <VersionPrefix>8.1.3</VersionPrefix>
    <Nullable>enable</Nullable>
    <Title>LiteOrm.Common</Title>
    <Authors>danjiewu</Authors>
    <PackageProjectUrl>https://danjiewu.github.io/LiteOrm</PackageProjectUrl>
    <PackageReadmeFile>README.nuget.md</PackageReadmeFile>
    <PackageTags>orm;sql;oracle;mysql;sql server;sqlite</PackageTags>
    <Description>LiteOrm.Common 公共库：LiteOrm 的共享核心，提供表达式树（Expr）、SQL 片段、表元数据（TableInfoProvider）、特性与公共接口，由 LiteOrm 包自动引用。</Description>
    <PackageReleaseNotes>
8.1.3:
      - 统一使用 DbValueType 替代 DbType，合并 DbTypeMap 至 DbValueTypeMap。
      - IDbConverter.GetDbType 改为 GetDbValueType；新增 GetDefaultLength。
      - DbValueType.Array 改为掩码（128），可与标量类型按位或组合。
      - 计算列支持 ValueTypeExpr 形式表达式。
      - DataReaderConverter 在 Default 时通过当前 SqlBuilder 推断 DbType。

8.1.2:
      - 优化 AOT 编译：移除全局裁剪告警抑制。
      - 新增 ColumnDefinition.ExpressionExpr：计算列支持 ValueTypeExpr 形式表达式（Expr 树），仅允许不生成参数的固定 SQL，优先于字符串形式 Expression。
      - ExprSqlConverter 移至 LiteOrm.Common。

8.1.1:
      - 破坏性变更：
      - [AutoRegister] 的 ServiceTypes 改为枚举 RegisterPolicy（All/Self/Interface，默认 All），原 Type[] 写法移除。
      - DbValueType 新增 Default/Jsonb/Array 枚举值；ColumnAttribute.DbType 与 ColumnDefinition.DbType 由 DbValueType? 改为非空 DbValueType（默认 DbValueType.Default，集合属性自动推断为 Array）；
      - IDbConverter.ConvertToDbValue 参数由 DbType 改为 DbValueType；
      - Param.DbType 类型由 DbType? 改为 DbValueType。
      - 新特性：
      - 新增数组类型支持：集合属性自动推断为 DbValueType.Array；
      - 新增 JSON/JSONB 类型支持（DbValueType.Json/Jsonb）；
      - 新增 JsonExprExtensions 公共 JSON 函数扩展；
      - Service 新增 Lambda 方式的 SearchAs / SearchOneAs / SearchAsAsync / SearchOneAsAsync 扩展；
      - 实体支持计算列（非实际列）：ColumnAttribute.Expression + ColumnMode.Computed。

8.1.0:
      - 破坏性变更：
      - PreparedSql 移至 LiteOrm.Common 项目，参数类型由 KeyValuePair 改为自定义 Param；
      - AttributeTableInfoProvider 不再依赖 SqlBuilderFactory、DataSourceProvider。
      - 新特性：
      - 新增 AOT / NativeAOT 支持：ExprJsonConverter、LambdaExprConverter 改为 AOT 安全实现，新增源生成的 ExprJsonSerializerContext；
      - ColumnDefinition.DbType 可为空，运行时自动判定 DbType 类型。
      - 改进：
      - 目标依赖包版本降低，减少版本冲突。
    </PackageReleaseNotes>
    <RepositoryUrl>https://github.com/danjiewu/LiteOrm.git</RepositoryUrl>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
  </PropertyGroup>
  <PropertyGroup>
    <LangVersion>14.0</LangVersion>
  </PropertyGroup>
  <PropertyGroup Condition="'$(TargetFramework)' == 'net8.0' OR '$(TargetFramework)' == 'net10.0'">
    <IsAotCompatible>true</IsAotCompatible>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)|$(TargetFramework)|$(Platform)'=='Release|netstandard2.0|AnyCPU'">
    <DocumentationFile>bin\Release\LiteOrm.Common.xml</DocumentationFile>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)|$(TargetFramework)|$(Platform)'=='Debug|netstandard2.0|AnyCPU'">
    <DocumentationFile>bin\Debug\LiteOrm.Common.xml</DocumentationFile>
    <OutputPath />
  </PropertyGroup>
  <ItemGroup>
    <Compile Remove="DAO\**" />
    <EmbeddedResource Remove="DAO\**" />
    <None Remove="DAO\**" />
  </ItemGroup>
  <ItemGroup>
    <None Include="..\README.nuget.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>
  <ItemGroup Condition="'$(TargetFramework)' == 'net10.0'">
    <PackageReference Include="System.Text.Json" Version="10.0.0" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetFramework)' == 'net8.0'">
    <PackageReference Include="System.Text.Json" Version="8.0.0" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.1' OR '$(TargetFramework)' == 'netstandard2.0'">
    <PackageReference Include="System.Text.Json" Version="8.0.0" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\LiteOrm.Generators\LiteOrm.Generators.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
    <None Include="..\LiteOrm.Generators\bin\$(Configuration)\netstandard2.0\LiteOrm.Generators.dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false" />
  </ItemGroup>
</Project>